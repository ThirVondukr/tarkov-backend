stages:
  - lint

.lint-job: &lint-job
  image: python:3.10-slim
  stage: lint
  cache:
    - key:
        files:
          - poetry.lock
      paths:
        - .cache/pip
        - .venv

  before_script:
    - pip install poetry
    - poetry config virtualenvs.in-project true
    - poetry install

pytest:
  <<: *lint-job
  script:
    - poetry run pytest
